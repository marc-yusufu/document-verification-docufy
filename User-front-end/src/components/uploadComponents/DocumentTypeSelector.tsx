export default function DocumentTypeSelector({ selectedType, onSelect }: {
    selectedType: string;
    onSelect: (type: string) => void;
  }) {
    return (
      <div className="...">
        {/* 3 type cards with onClick={() => onSelect("residence")} */}
      </div>
    );
  }
  